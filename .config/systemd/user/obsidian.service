[Unit]
Description=Obsidian (headless-capable GUI for CLI backend)
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/obsidian --ozone-platform=x11
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
