# dotfiles

## ワークフロー

- 何か修正する際は必ず作業ブランチを作成すること。`master` に直接コミットしない。
- 修正が完了したら、ブランチを `origin` に push し、PR 経由で `master` にマージする。
  - ローカルでの直接マージ + push は Claude Code が `master` への直接 push をブロックするため不可。
